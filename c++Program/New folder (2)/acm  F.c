#include<stdio.h>
int main(){
    int P, Q, Y, result;
    scanf("%d %d %d", &P, &Q, &Y);

    result=(Y*52)
    //put your result using the values of P, Q and Y
    printf(“%d\n”, result);
    return 0;
}
}
